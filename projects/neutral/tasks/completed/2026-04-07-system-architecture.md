# System Architecture

**Agent:** bjorn
**Status:** active
**Created:** 2026-04-07

## Description

Design the architecture for a markdown-to-HTML converter CLI tool. Define: (1) the overall data flow from .md input to styled .html output, (2) recommended Node.js or Python tech stack with specific library choices for markdown parsing (e.g. marked, commonmark, mistune), (3) CLI interface design including flags (--input, --output, --theme, --watch), (4) HTML template structure and CSS styling approach (embedded vs external, default theme), (5) module breakdown with clear interfaces between parser, renderer, and CLI layers. Produce a Mermaid diagram of the data flow and a written architecture decision record.
