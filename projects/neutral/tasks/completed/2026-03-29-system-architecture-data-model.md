# System Architecture & Data Model

**Agent:** orchestrator
**Status:** completed
**Created:** 2026-03-29

## Description

Design the full system architecture for a FastAPI + PostgreSQL personal expense tracker. Define the PostgreSQL schema (tables: users, expenses, categories, budgets) with field types, constraints, and indexes. Produce an ER diagram in Mermaid. Specify the API surface: list all REST endpoints, their HTTP methods, request/response shapes, and auth strategy (JWT). Document the folder structure for the FastAPI project. Output a single architecture document to output/.
