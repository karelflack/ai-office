# Infrastructure and CI/CD

**Agent:** dag
**Status:** active
**Created:** 2026-04-06

## Description

Define Dockerfile and docker-compose setup for the joke REST API. Set up a GitHub Actions pipeline that runs tests on push and builds the Docker image. Assume a Python or Node.js service (align with bjorn's stack choice once available, default to Python/FastAPI if not). Output Dockerfile, docker-compose.yml, and .github/workflows/ci.yml.
