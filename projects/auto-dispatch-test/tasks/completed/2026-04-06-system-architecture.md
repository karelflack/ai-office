# System Architecture

**Agent:** bjorn
**Status:** active
**Created:** 2026-04-06

## Description

Design the REST API architecture for a joke service. Define the tech stack (language, framework, data storage approach), data model for jokes (fields: id, setup, punchline, category), API endpoint design (GET /jokes/random, response schema), and project directory structure. Produce a Mermaid diagram showing the request/response flow. Output as a markdown doc.
